# MinIO Environment Variables
# These should match your backend/.env settings

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Optional: Set this if using reverse proxy (Nginx/Traefik)
# MINIO_BROWSER_REDIRECT_URL=https://minio-console.yourdomain.com

# Optional: Domain configuration for production
# MINIO_SERVER_URL=https://minio-api.yourdomain.com
# MINIO_BROWSER_REDIRECT_URL=https://minio-console.yourdomain.com
