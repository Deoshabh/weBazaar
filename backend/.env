PORT=5000
NODE_ENV=development

# Database
MONGO_URI=mongodb://127.0.0.1:27017/shoes_auth

# Client
CLIENT_URL=http://localhost:3000

# JWT Secrets (replace before running in shared environments)
JWT_SECRET=replace-with-local-dev-secret
JWT_ACCESS_SECRET=replace-with-local-dev-access-secret
JWT_REFRESH_SECRET=replace-with-local-dev-refresh-secret

# JWT Expiration
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Razorpay
RAZORPAY_KEY_ID=replace-with-razorpay-key-id
RAZORPAY_KEY_SECRET=replace-with-razorpay-key-secret

# MinIO
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=kzw03p43vnzwsq1t
MINIO_BUCKET=product-media
MINIO_REGION=us-east-1

# Shiprocket
SHIPROCKET_EMAIL=replace-with-shiprocket-email
SHIPROCKET_PASSWORD=replace-with-shiprocket-password
SHIPROCKET_WEBHOOK_TOKEN=replace-with-webhook-token
# SHIPROCKET_WEBHOOK_SECRET=replace-with-webhook-secret

# Soketi
SOKETI_APP_ID=replace-with-soketi-app-id
SOKETI_APP_KEY=replace-with-soketi-app-key
SOKETI_APP_SECRET=replace-with-soketi-app-secret
SOKETI_CLUSTER=mt1
SOKETI_HOST=localhost
SOKETI_PORT=6001
SOKETI_USE_TLS=false

# Firebase Admin SDK (for backend authentication)
# Option 1: Use service account file path (recommended for local development)
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Option 2: Use individual credentials (for production/Docker)
# FIREBASE_PROJECT_ID=webazaar-62921
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@webazaar-62921.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# ====================
# reCAPTCHA Enterprise
# ====================
# ⚠️  CRITICAL: reCAPTCHA verification is temporarily DISABLED until credentials are configured
# To properly set this up:
# 1. Go to https://console.cloud.google.com/
# 2. Create a Google Cloud Project (or use existing)
# 3. Enable reCAPTCHA Enterprise API
# 4. Create a Service Account with "reCAPTCHA Enterprise Admin" role
# 5. Create and download a JSON key for the service account
# 6. Save the JSON file as: backend/google-credentials.json
# 7. Update GOOGLE_CLOUD_PROJECT_ID below with your actual project ID
# 8. Re-enable reCAPTCHA middleware in backend/routes/authRoutes.js

# Google Cloud Project ID for reCAPTCHA Enterprise
# REPLACE WITH YOUR ACTUAL PROJECT ID FROM GOOGLE CLOUD CONSOLE
GOOGLE_CLOUD_PROJECT_ID=your-actual-google-cloud-project-id

# Path to Google service account credentials JSON file
# This file should have reCAPTCHA Enterprise Admin permissions
# File must be placed at: backend/google-credentials.json
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# reCAPTCHA Site Key (from Google Cloud Console)
RECAPTCHA_SITE_KEY=6LcbjmUsAAAAAHVeGta063p2ii-OlYGQqOBPfmQl

# Valkey / Redis Configuration
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=erhbztkuyvv5xlnljijzeoopkpp1jvuv
