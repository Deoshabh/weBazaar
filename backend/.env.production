# Production Environment Variables for Backend
# Domain: radeo.in

# Server Configuration
PORT=5000
NODE_ENV=production

# Database - Dokploy MongoDB Service
MONGO_URI=mongodb://admin:StrongPassword123@mongo-db-mongodb-avaxdz:27017/radeo_production?authSource=admin

# Client URL (Your frontend domain)
CLIENT_URL=https://radeo.in

# JWT Secrets - CHANGE THESE IN PRODUCTION!
# Generate secure secrets using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_STRING_32_CHARS_MIN
JWT_ACCESS_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_STRING_32_CHARS_MIN
JWT_REFRESH_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_STRING_32_CHARS_MIN

# JWT Expiration
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Razorpay - Replace with your production keys
RAZORPAY_KEY_ID=rzp_test_SBfFobfvhPtFYL
RAZORPAY_KEY_SECRET=vTZJIY2T5zJmIqRk7aeuidrJ

# MinIO Object Storage
# Option 1: MinIO on same VPS
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=product-media
MINIO_REGION=us-east-1

# Option 2: MinIO with subdomain (if configured)
# MINIO_ENDPOINT=minio.radeo.in
# MINIO_PORT=443
# MINIO_USE_SSL=true
# MINIO_ACCESS_KEY=your-secure-access-key
# MINIO_SECRET_KEY=your-secure-secret-key
# MINIO_BUCKET=product-media
# MINIO_REGION=us-east-1

# Option 3: AWS S3 (Alternative to MinIO)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=ap-south-1
# AWS_S3_BUCKET=radeo-product-images

# Email Configuration (Optional - for order confirmations)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# FROM_EMAIL=noreply@radeo.in

# Security
# CORS_ORIGIN=https://radeo.in,https://www.radeo.in
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
