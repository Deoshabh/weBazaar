version: '3.8'

services:
  # Node.js Backend
  backend:
    build: ./backend
    container_name: weBazaar-backend
    restart: always
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=${MONGO_URI}
      - REDIS_URL=${REDIS_URL}
      - MINIO_ENDPOINT=${MINIO_ENDPOINT}
      - MINIO_PORT=${MINIO_PORT}
      - MINIO_USE_SSL=${MINIO_USE_SSL}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
      - MINIO_BUCKET_NAME=weBazaar-reviews
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      - mongo
      - redis
      - minio
    networks:
      - weBazaar-network

  # Python AI Worker
  ai-worker:
    build: ./ai-worker
    container_name: weBazaar-ai-worker
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - MINIO_ENDPOINT=minio:9000
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY:-minioadmin}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY:-minioadmin}
      - MINIO_BUCKET_NAME=weBazaar-reviews
      - MONGO_URI=mongodb://mongo:27017/weBazaar
    depends_on:
      - redis
      - minio
      - mongo
    networks:
      - weBazaar-network

  # Frontend (Next.js)
  frontend:
    build: ./frontend
    container_name: weBazaar-frontend
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
    depends_on:
      - backend
    networks:
      - weBazaar-network

  # Database
  mongo:
    image: mongo:6.0
    container_name: weBazaar-mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - weBazaar-network

  # Queue (Valkey/Redis)
  redis:
    image: valkey/valkey:7.2
    container_name: weBazaar-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - weBazaar-network

  # Object Storage
  minio:
    image: minio/minio
    container_name: weBazaar-minio
    restart: always
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ACCESS_KEY:-minioadmin}
      - MINIO_ROOT_PASSWORD=${MINIO_SECRET_KEY:-kzw03p43vnzwsq1t}
      - MINIO_BROWSER_REDIRECT_URL=https://minio.webazaar.in
    volumes:
      - minio_data:/data
    networks:
      - weBazaar-network

networks:
  weBazaar-network:
    driver: bridge

volumes:
  mongo_data:
  redis_data:
  minio_data:
